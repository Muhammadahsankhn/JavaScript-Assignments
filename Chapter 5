<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="matter.js"></script>
</head>
<body>
    <script src="matter.js"></script>
</body>
</html>-->












//                      Qno 1

// var num1 = 3;
// var num2 = 5;

// var sum = num1 + num2;

// document.body.innerHTML = `The sum of ${num1} and ${num2} is ${sum}.`;






//                      Qno 2



// var divi = num1 / num2;
// var mult = num1 * num2;
// var subb = num1 - num2;
// var modul = num1 % num2;

// document.body.innerHTML += `<br>The division of ${num1} and ${num2} is ${divi}.`;
// document.body.innerHTML += `<br>The multiple of ${num1} and ${num2} is ${mult}.`;
// document.body.innerHTML += `<br>The subbtraction of ${num1} and ${num2} is ${subb}.`;
// document.body.innerHTML += `<br>The Modulus of ${num1} and ${num2} is ${modul}.`;









//                      Qno 3



// var value 

// document.body.innerHTML += `<br>Value After varriable declaration is ${value}.`;

// var value = 5;
// document.body.innerHTML += `<br>Initial Value ${value}.`;

// var value = ++value;
// document.body.innerHTML += `<br>Value after increament ${value}.`;

// var value = value + 7;
// document.body.innerHTML += `<br>Value after Addition ${value}.`;

// var value = --value; 
// document.body.innerHTML += `<br>Value after decreament ${value}.`;

// var value = value % 3;
// document.body.innerHTML += `<br>Remainder is ${value}.`;







//                      Qno 4


// var ticket = 600;

// var buy = 5;

// document.body.innerHTML += `<br>Total cost of buy ${buy} ticket to a movie is ${ticket*buy}PKR.`;






//                      Qno 5


// var table = 4;

// document.body.innerHTML += `<br>Table of ${table}`;
// for (var i = 1; i <= 10; i++) {
//     var result = table * i;
//     document.body.innerHTML += `<br>${table} x ${i} = ${result}`;
// }









//                      Qno 6


// var celcius = 25;

// var fahrenheit = (celcius * 9/5) + 32;

// document.body.innerHTML += `<br>${celcius}°C is equal to ${fahrenheit}°F`;



// var fahrenheit = 70;

// var celcius = (fahrenheit - 32) * 5/9

// document.body.innerHTML += `<br>${fahrenheit}°F is equal to ${celcius.toFixed(2)}°C`;








//                      Qno 7


// var price1 = 650;

// var quantity1 = 3;

// var price2 = 100;

// var quantity2 = 7;

// var charges = 100;

// var total = (price1*quantity1)+(price2*quantity2)+charges;

// document.body.innerHTML += `<br>Price of item 1 is ${price1}.
//                             <br>Quantity of item 1 is ${quantity1}.
//                             <br>price of item 2 is ${price2}.
//                             <br>Quantity of item 2 is ${quantity2}.
//                             <br>Shipping charges is ${charges}.
//                             <br>Total is ${total}.
//                             `;










//                      Qno 8



// var totalMarks = 980;

// var obtainMarks = 804;

// var percentage = (obtainMarks*100)/totalMarks

// document.body.innerHTML += `<br>Total marks :${totalMarks}.
//                             <br>Obtained marks :${obtainMarks}
//                             <br>Percentage :${percentage}
//                             `;













//                      Qno 9



// document.body.innerHTML += `<br>Total currency in PKR :${(10*104.80)+(25*28)}`;











//                      Qno 10


// var number = 5;

// var result = (((number+5)*10)/2);

// document.body.innerHTML += `<br>${result}`;







//                      Qno 11


// var cyear = 2024;
// var byear = 2005;
// var age = cyear - byear;

// document.body.innerHTML += `<br>Current year ${cyear}.
//                             <br>Birth year ${byear}.
//                             <br>Your age is :${age}.
//                             `;






//                      Qno 12


// var radius = 20;

// var circum = (2*3.142*20);

// var area = (3.142*20*20);

// document.body.innerHTML += `<br>Radius of circle is :${radius}.
//                             <br>Circumference of circle is ${circum}.
//                             <br>Area of circle is ${area}.
//                             `;













//                      Qno 13


// var fsnack = "Snackers Cocalate";

// var age = 19;

// var maxAge = 70;

// var quantity = 3;

// var total = (maxAge-age)*365*quantity;

// document.body.innerHTML += `<br>Favourite snack :${fsnack}.
//                             <br>Current age :${age}.
//                             <br>Maximum age :${maxAge}.
//                             <br>Ammount of snack per day :${quantity}.
//                             <br>You need ${total} ${fsnack} to last you until the ripe old age of ${maxAge}.
//                              `;
